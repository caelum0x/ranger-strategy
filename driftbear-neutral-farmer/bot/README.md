# Bot

Starter bot scaffold for the DriftBear neutral allocation model.

## Files

- `.env.example`: runtime variables.
- `strategies.json`: strategy declarations.
- `src/rebalance_loop.ts`: 50/50 neutral rebalance stub.

Use this scaffold if you want a separate deployable keeper repo instead of the integrated agent already present in the main codebase.
